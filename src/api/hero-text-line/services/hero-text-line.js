/**
 * hero-text-line service
 */
const { factories } = require('@strapi/strapi');

module.exports = factories.createCoreService('api::hero-text-line.hero-text-line', ({ strapi }) => ({
  // Add custom service methods here
}));
